export type GiftBackdrop = {
  name: string;
  centerColor: number;
  edgeColor: number;
  patternColor: number;
  textColor: number;
  rarityPermille: number;
  hex: {
    centerColor: string;
    edgeColor: string;
    patternColor: string;
    textColor: string;
  };
};
